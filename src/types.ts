export interface Config {
  imageWidth: number;
  imageHeight: number;
  patternWidth: number;
  patternHeight: number;
  pageWidth: number;
  pageHeight: number;
  textSize: number;
  textColor: string;
  textOffsetX: number;
  textOffsetY: number;
  lineWidth: number;
}
